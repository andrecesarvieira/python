from tkinter import *
from tkinter import ttk

# A classe Tk() será instancia para a janela principal da aplicação
mainFrm = ttk.Frame()
lbl_mainFrm = Label(mainFrm)

# frm_mainFrm = ttk.Frame(mainFrm, padding=10)

# frm_mainFrm.grid()

# frm_mainFrm = ttk.Label(text="Teste", background="gray").grid(column=0, row=0)
# ttk.Label(frm, text="Teste", background="gray").grid(column=0, row=0)

mainFrm.mainloop()
